{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Launch PhxRuntime",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: Build Current Preset"
    }
  ]
}
